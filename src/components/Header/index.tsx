import React from 'react';
import { SearchComponent } from '../SearchComponent';

const Header: React.FC = () => {
  return (
    <header>
      <SearchComponent />
    </header>
  );
};

export { Header };
